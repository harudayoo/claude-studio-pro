# {{PROJECT_NAME}}

{{STACK_LINE}}

## Commands
- Dev: `{{DEV_COMMAND}}`
- Test: `{{TEST_COMMAND}}`   Single: `{{SINGLE_TEST_COMMAND}}`
- Lint/format: `{{FORMAT_COMMAND}}`   Types: `{{TYPECHECK_COMMAND}}`
- Build: `{{BUILD_COMMAND}}`   Dependency audit: `{{DEPENDENCY_AUDIT_COMMAND}}`

## Layout
- Source: `{{SOURCE_ROOTS}}`
- Front-end: `{{FRONTEND_ROOT}}`
- Tests: `{{TEST_ROOT}}`
- Design tokens: `{{TOKEN_FILE}}` — the whole surface re-themes from here
- Specs, decisions, reports: `docs/specs/`, `docs/adr/`, `docs/reports/`

## Non-negotiables
- No source edits before an approved plan exists at `docs/specs/<slug>/plan.md`.
  Check `.claude/state/gate.json`.
- No phase closes on an assertion. Each closes on a file that exists and is
  non-empty. A summary in chat is not an artifact.
- Every bug fix starts with a failing test. Findings that become work re-enter
  at TEST, not at CREATE.
- No raw colour, spacing, radius or font-size value in a component. Use tokens.
- Every schema change ships a reversible migration and a test.
- Authorisation, payments, destructive migrations, public API contracts, PII
  handling and rate limiting require `/debate` before the plan is approved.
- Never edit `.env`, build output, or vendored dependencies.

<!-- studio:frontend-direction:start -->
## Frontend direction
<always_use_this_direction>
Always design with this aesthetic:
- REPLACE ME: name one direction (editorial, Swiss minimal, brutalist,
  glassmorphic, dark OLED luxury, retro-futurist, organic, art deco, SaaS
  minimal), then list three to five concrete consequences of it.
</always_use_this_direction>
This block outranks any design skill's preference.
Adoption procedure: https://github.com/harudayoo/claude-studio-pro/blob/main/docs/DESIGN-STACK.md
<!-- studio:frontend-direction:end -->

## Working agreement
- Start any change with the `conductor` agent. It owns phase order; it never
  writes application code.
- Run exactly one phase per turn. Never skip forward.
- Run `/handoff` before ending a session with uncommitted work.
- Use `/clear` between phases and between unrelated tasks.

## Compact instructions
When compacting, preserve: the active gate state, decisions made this session,
and file paths already discovered. Discard tool output and exploration
transcripts.
