# claude-studio — Max 20x tier manifest
#
# Sourced by install.sh, configure.sh and verify.sh.
#
# Max 20x: the full 24-agent roster from docs/DESIGN-RATIONALE.md. Every phase
# has its own playbook skill, VERIFY fans out to five read-only auditors from a
# single dispatch, debate escalates to three levels including agent teams, and
# the monthly /studio-report runs on OpenTelemetry data rather than anecdote.
#
# This tier costs real money. Install it because you can name the failure each
# extra agent prevents, not because the roster is longer.

TIER_ID="max20x"
TIER_NAME="Max 20x"
TIER_TAGLINE="24 agents, 5-way parallel verify, 3-level debate, telemetry"
TIER_BLURB="The complete roster: specialist designers per surface, five-lens parallel verification, agent-team escalation for genuinely uncertain work, and a measured monthly report."

TIER_AGENTS="conductor \
product-analyst ux-researcher \
system-architect data-architect api-designer ui-designer security-architect devops-planner red-team-critic \
test-designer a11y-perf-budgeter \
backend-engineer frontend-engineer db-engineer infra-engineer \
code-reviewer security-auditor qa-runner perf-a11y-auditor seo-auditor \
doc-writer memory-curator report-generator"

TIER_SKILLS="phase-idea phase-plan phase-test phase-create phase-verify phase-document \
debate handoff codebase-map design-tokens studio-report web-audit ci-scaffold security-audit"

TIER_RULES="backend frontend database testing security seo devops"

TIER_PHASES="idea plan test create verify document"

TIER_OPUS_AGENTS="conductor system-architect data-architect security-architect red-team-critic security-auditor"

TIER_UI_AGENTS="ux-researcher ui-designer frontend-engineer qa-runner perf-a11y-auditor seo-auditor a11y-perf-budgeter"
TIER_UI_SKILLS="web-audit design-tokens"
TIER_UI_RULES="frontend seo"

TIER_EXTRA_CLAUDE_DIRS=".claude/workflows"
TIER_EXTRA_DOC_DIRS="docs/design docs/api docs/runbooks"

TIER_ENTRY_COMMAND="use the conductor agent to start: <description>"
